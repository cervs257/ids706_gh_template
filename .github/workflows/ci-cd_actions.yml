name: ci-cd_actions
on: push

jobs:
build:
  runs-on: ubuntu-latest
  steps:
    - name: setup python
      uses: actions/setup-python@v4
      with: 
        python-version: '3.10.12'
    # - name: install packages
    # run: make install
    # run: make lint
    # run: make test
    # format
    # deploy
